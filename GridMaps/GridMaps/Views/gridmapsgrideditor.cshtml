@using System.Web.Mvc.Html
@using EventCalendarBelle.Extensions
@inherits Umbraco.Web.Mvc.UmbracoViewPage<dynamic>
@if (Model.value != null)
{
    string value = Model.value.ToString();
    @value
    var id = new Guid().ToString() + "_map";
    <div class="map" id="@id"></div>
}